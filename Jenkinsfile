pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Building project..."'
                sh 'ls -la'
                // todo: add build stuff
            }
        }
        stage('Test') {
            steps {
                sh 'echo "Running tests..."'
                // sh 'make test'
            }
        }
    }
}